services:
  postgresql-enib:
    image: postgres:14
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "enib"
      POSTGRES_USER: "enib"
      POSTGRES_PASSWORD: "enib"
    volumes:
      - ./data:/var/lib/postgresql/data
      - ./src/main/resources/sql/init.sql:/docker-entrypoint-initdb.d/init.sql:ro